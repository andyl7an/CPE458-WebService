from django.db import models

# Create your models here.
"""
class Location(models.Model):
    store_name = models.CharField(max_length=30)
    street_addr = models.CharField(max_length=30, default="SOME STRING")
    """